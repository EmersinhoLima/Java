package estojo;


public class Caneta {

	  String modelo;
	  String cor;
	  float ponta;
	  int carga;
	  boolean tampada;
	  String material;
	  boolean rabiscar;
	  
	  void cores() {
			 System.out.println("Qual é a cor da Caneta? " + cor  );
			 
	  public void status() {
		 System.out.println("qual é a ponta da caneta? " + ponta );	
	  }
	  void rabiscar() {
		  	System.out.println("Qual o modelo ? " + modelo );	
	  }

	  void tampar() {
		  	System.out.println("Ela rabisca? " + tampada );	
			    }
	  
}

